// Copyright © 2017-2021 Trust Wallet.
//
// This file is part of Trust. The full Trust copyright notice, including
// terms governing use, modification, and redistribution, is contained in the
// file LICENSE at the root of the source code distribution tree.

#include <NiceWCore/NWAnyAddress.h>
#include "HexCoding.h"

#include "../interface/NWTestUtilities.h"
#include <gtest/gtest.h>

using namespace NW;

// TODO: Finalize tests

TEST(NW<%= name %>, Address) {
    // TODO: Finalize test implementation

    auto string = STRING("__ADD_VALID_ADDRESS_HERE__");
    auto addr = WRAP(NWAnyAddress, NWAnyAddressCreateWithString(string.get(), NWCoinType<%= format_name(coin) %>));
    auto string2 = WRAPS(NWAnyAddressDescription(addr.get()));
    EXPECT_TRUE(NWStringEqual(string.get(), string2.get()));
    auto keyHash = WRAPD(NWAnyAddressData(addr.get()));
    assertHexEqual(keyHash, "__CORRESPONDING_ADDRESS_DATA__");
}
